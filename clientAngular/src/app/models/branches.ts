export interface Branches {
    _id: string,
    name: string,
    city: string,
    address: string,
    phoneNumber: string,
    lat: Number,
    long: Number,
    isOpen: Boolean,
    stateOpen: string, 
    shop: string,
    orders: string    
}